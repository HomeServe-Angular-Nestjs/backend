export enum ReportStatus {
  PENDING = 'pending',
  REJECTED = 'rejected',
  RESOLVED = 'resolved',
}

export enum ComplaintReason {
  SPAM = 'spam',
  INAPPROPRIATE = 'inappropriate',
  HARASSMENT = 'harassment',
  OTHER = 'other',
}
