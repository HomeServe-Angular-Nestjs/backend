export enum SubsDurationType {
    Monthly = 'monthly',
    Yearly = 'yearly',
}

export enum PlanRoleEnum {
    Customer = 'customer',
    Provider = 'provider',
}

export enum RenewalEnum {
    Auto = 'auto',
    Manual = 'manual',
}